import { iUserData } from "../interfaces/iUserData";
export declare const eventBusUserLogin: {
    subscribe: (callback: (user: iUserData) => Promise<void>) => () => void;
    broadcast: (user: iUserData) => Promise<void>;
};
