import { iNode } from "./iNode";
export interface iPagingItems<TItems, TNode = {
    [key: string]: unknown;
} | undefined> {
    node?: iNode<TNode>;
    totalItems: number;
    perPage: number;
    items: TItems[];
}
