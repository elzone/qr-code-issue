type OpenCallback = () => Promise<void>;
type MessageCallback = (message: string) => void;
export declare class WebSocketClient {
    private url;
    private websocket;
    private reconnectInterval;
    private reconnectAttempts;
    private maxReconnectAttempts;
    private isReconnecting;
    private sendMessageBuffer;
    private openCallback;
    private messageCallback;
    constructor(url: string, reconnectInterval?: number, maxReconnectAttempts?: number);
    connect(): void;
    private reconnect;
    send(message?: string, priority?: boolean): void;
    onOpen(callback: OpenCallback): void;
    onMessage(callback: MessageCallback): void;
    close(): void;
}
export {};
